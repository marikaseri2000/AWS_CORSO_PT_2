---
trigger: always_on
---

Every task, should be realized working in a new branch starting from the main one. Always check if the local main is aligned with origin one. Each branch should have the task name.