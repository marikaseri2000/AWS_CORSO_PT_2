---
trigger: always_on
---

Every task, should be realized working in a new branch strating from the main one. Always chech if the local main is aligned with origin one. Each branch should have the task name.